# PipelineIQ — Python dependencies
# No API keys required. All inference is local via Ollama.

# RAG & Embeddings (local, no API key)
sentence-transformers==3.0.0   # Local embeddings — downloads ~80MB model once
chromadb==0.5.15               # Persistent local vector store

# LLM (via Ollama — install separately)
requests==2.32.3               # HTTP client for Ollama API

# Optional: REST API server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Utilities
python-dotenv==1.0.1
